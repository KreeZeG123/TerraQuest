{% extends 'base.html.twig' %}

{% block title %}TerraQuest - Admin{{ title }}{% endblock %}

{%  block body %}
    <nav class="navbar navbar-dark bg-dark px-5 fixed-top justify-content-between">
        <a type="button" class="btn bsb-btn-xl btn-primary gap-3" href="{{ path('admin.entity.index', {entityType: entityType}) }}">Retours</a>
        <h3 class="text-white mx-auto text-center">Menu Administrateur</h3>
    </nav>
    <main class="d-flex flex-column gap-4 py-2 px-5 nav-margin-top">

        {{ form(form) }}
    </main>
{% endblock %}